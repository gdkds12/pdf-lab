# Phase 1 개발 문서 (교재 구조화)

## 목적
- 스캔 PDF/디지털 PDF를 구조화하여 이후 추천 엔진에서 참조 가능한 교재 좌표를 만든다.
- 핵심은 원문 재출력이 아니라 `page`, `anchor`, `chunk_id`를 안정적으로 확보하는 것이다.

## 입력
- 사용자 업로드 교재(PDF)

## 처리
1. OCR/텍스트 추출
2. 섹션/문제/예제 단위 chunking
3. `chunks` 저장
   - `chunk_id`, `source_id`, `page_start/end`, `anchor_path`, `content_text`
4. 임베딩 저장(pgvector + halfvec)

## 출력 계약
- `chunks` 테이블에 참조 가능한 최소 메타를 반드시 포함
- 이후 단계(Phase 3/4)가 `chunk_id` 기반으로만 연결 가능해야 함

## 품질 기준
- 페이지 범위 누락률 최소화
- anchor_path 누락 시 page 기반 fallback 허용
- OCR 실패 chunk는 별도 로그 남김

## 저작권/보안
- 원문 재출력 API 제공 금지
- 처리 완료 후 보존 정책(TTL/세션 삭제 정책) 준수

## 현재 상태
- 구현 완료(MVP)
- Phase 3/4 연동 가능한 메타 스키마 적용 완료
