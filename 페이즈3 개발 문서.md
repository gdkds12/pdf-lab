# Phase 3 개발 문서 (Hybrid Retrieval / Map)

## 목적
- Phase 2 신호를 교재 chunk 후보와 대량 매칭한다.
- Precision보다 Recall 우선으로 후보를 충분히 모은다.

## 입력
- `signals.search_queries`
- Phase 1에서 저장한 `chunks` + 임베딩

## 처리
1. query 정규화/중복 제거
2. 임베딩 생성
3. 하이브리드 검색
   - 벡터 검색 + 키워드 검색
   - RRF로 통합 랭킹
4. `evidence_candidates` 저장

## 출력 계약 (`evidence_candidates`)
- `session_id`, `signal_id`, `chunk_id`
- `query_used`, `rrf_score`, rank 정보

## 품질 기준
- 신호당 근거 후보 충분히 확보
- 후보 저장 실패 시 재시도/에러 로그
- 근거 없는 신호는 Phase 4 경고로 전달

## 현재 상태
- 구현 완료(MVP)
- RRF 기반 하이브리드 검색 파이프라인 운영 중
