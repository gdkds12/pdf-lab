# Phase 4 개발 문서 (추천 큐 생성 / Reduce)

## 목적
- Phase 2~3에서 모은 신호와 근거 후보를 통합해
  `추천 문제 큐(Recommendation Queue)`를 생성한다.
- 이 단계는 "시험 예측 결론"을 내리지 않는다.

## 입력
- `signals` (타임라인)
- `evidence_candidates`
- `chunks` (page/anchor 포함)

## 출력 스키마 (`session_reports.report_json`)
```json
{
  "recommendation_queue": [
    {
      "rank": 1,
      "title": "3장 2절 연습문제 7번",
      "why": "교수 반복 강조 + 교재 핵심 개념 연결",
      "study_action": "핵심 정의 확인 후 문제 풀이",
      "importance": 0.92,
      "importance_score": 92,
      "proof_refs": [
        {
          "signal_id": "...",
          "audio_chunk_id": "...",
          "t0_sec": 1390.2,
          "t1_sec": 1408.1,
          "note": "반복 강조"
        }
      ],
      "references": [
        {
          "chunk_id": "...",
          "page_start": 124,
          "page_end": 124,
          "anchor_path": ["3장", "2절", "연습문제"],
          "reason": "교수 발화와 동일 유형"
        }
      ]
    }
  ],
  "warnings": []
}
```

## 핵심 검증 규칙
- importance 0.3 미만 제거
- `proof_refs.signal_id`는 실제 신호와 매칭되어야 함
- `references.chunk_id`는 실제 chunk와 매칭되어야 함
- 오디오 근거 또는 교재 근거 중 하나라도 없으면 제외
- 원문 인용 탐지 시 요약 문장으로 대체

## 정렬/중복 제거
- title + reference chunk 기반 dedup
- importance/근거 수 기준 정렬
- `PHASE4_MAX_QUEUE_ITEMS` 상한 적용

## 상태 전이
- 시작: `sessions.status = reasoning`
- 완료: `sessions.status = completed`
- 실패: `sessions.status = failed`

## 현재 상태
- 구현 완료(MVP)
- vNext 스키마(`recommendation_queue`) 전환 완료
