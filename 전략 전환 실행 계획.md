# AI 출제 신호 기반 학습 내비게이션 전환 실행 계획

## 0) 목적
- 기존 Project Thunder의 핵심 기술(Phase 1~4 Map-Reduce 파이프라인, 신호 추출/근거 매칭/최종 리포트 생성)은 유지한다.
- 서비스 포지셔닝을 **"교재 대체"가 아닌 "학습 우선순위 내비게이션"**으로 전환한다.
- 저작권 리스크를 구조적으로 낮추기 위해 데이터/출력/UX 정책을 제품 전반에 반영한다.
- 프론트엔드는 정보 구조(IA)부터 재설계해 현재 혼재된 화면을 단순한 학습 내비게이션 플로우로 통합한다.

---

## 1) 전략서 반영을 위한 변경 축 (What to change)

## 1-1. 제품 메시지/도메인 언어 전환
- 변경 전: "시험 예측", "요약", "문서 분석" 중심 표현
- 변경 후: "출제 신호", "학습 우선순위", "근거 위치 안내" 중심 표현

### 즉시 반영 대상
- 랜딩/로그인/대시보드 카피 (`web/app/page.tsx`, `web/app/login/page.tsx`, 대시보드 컴포넌트)
- 리포트 섹션명 및 설명 문구 (`likely`, `trap_warnings`를 사용자 친화 라벨로 매핑)
- README/운영 문서의 서비스 설명 문구

## 1-2. 법적 리스크 제어를 위한 제품 규칙 고정
- "교재 원문 재현 방지"를 기능 요구사항으로 명문화한다.
- 서버 저장 정책을 "세션 기반 최소 보관 + 만료/삭제"로 명시한다.
- 출력은 원문 대신 **위치(anchor_path/page 범위) + 요약된 근거 포인트**만 제공한다.

### 즉시 반영 대상
- 백엔드 출력 스키마: 직접 인용 길이 제한, 금칙 패턴 필터, citation 중심 구조
- 프론트 리포트 뷰: "원문 보기" 대신 "교재 위치 보기" 중심 UI
- 이용약관/동의 플로우: 사용자 권리 보증 및 업로드 책임 고지

## 1-3. 기존 기술 유지 + 전략 적합성 강화
- 유지할 것:
  - Phase 2 신호 추출
  - Phase 3 하이브리드 검색 및 evidence 후보 축적
  - Phase 4 통합 추론(단, 출력 정책 변경)
- 강화할 것:
  - 학기 단위 누적 신호 분석(반복/강조 패턴 가중)
  - 신뢰도 계산 근거(다중 에이전트 합의) 노출

### 즉시 반영 대상
- `backend/src/phase2`: 신호 타입/강도 스키마 확장
- `backend/src/phase3`: 구조 단위(anchor_path) 가중치 반영
- `backend/src/phase4`: 결과를 "학습 우선순위 큐"로 재구성

---

## 2) 프론트엔드 재작성 계획 (IA + 화면 플로우)

## 2-1. 핵심 사용자 여정(단일 플로우)
1. 과목 생성
2. 강의 오디오 업로드
3. 교재 파일 업로드(세션 한정 처리 고지)
4. 분석 실행
5. 학습 내비게이션 카드 확인(우선순위/근거 위치/신뢰도)
6. 개인 학습 체크리스트로 전환

## 2-2. 화면 구조(재작성 권장)
- `/` 랜딩: 포지셔닝, 법적 안전장치, 실제 효용(학습 시간 단축) 강조
- `/dashboard` 과목 목록: "시험 예측"이 아니라 "학습 내비게이션 프로젝트" 목록
- `/dashboard/[id]` 워크스페이스를 3패널로 단순화
  - 좌측: 세션/업로드 상태
  - 중앙: 우선순위 카드(Top N)
  - 우측: 근거 위치/신뢰도/학습 메모

## 2-3. 제거/축소 권장 요소
- 교재 원문을 길게 노출하는 뷰
- PDF 질의형 인터페이스로 오해될 수 있는 채팅 UX
- "정답/문제 생성"으로 보이는 CTA 문구

---

## 3) 백엔드/데이터 정책 변경 계획

## 3-1. 출력 포맷 전환
- 기존: 예측 리포트 중심 JSON
- 변경: 학습 내비게이션 JSON
  - `priority_level` (high/medium/low)
  - `why_now` (학기 맥락 근거)
  - `study_action` (학습 액션)
  - `evidence_refs` (page/anchor/timecode)
  - `confidence` + `agent_consensus`

## 3-2. 저작권 안전 필터
- 직접 인용 최대 길이 제한
- 연속 문장 재현 패턴 감지 시 마스킹
- 금지 출력 유형(요약본 대체, 장문 재구성) 차단

## 3-3. 데이터 보존 정책
- 세션 종료 후 원문/전처리 산출물 TTL 삭제 정책 도입
- 감사 로그는 메타데이터 중심(요청/응답 길이, 필터 트리거 여부)

---

## 4) 구현 로드맵 (기술 최대 유지)

## Phase A (1~2주): 포지셔닝/리스크 방어선
- 카피 전면 교체(랜딩/대시보드/리포트)
- 출력 필터 1차 적용(인용 길이/패턴 마스킹)
- 정책 문서/약관 업데이트

## Phase B (2~4주): 프론트 재작성
- 대시보드 정보 구조 재설계
- 기존 컴포넌트에서 업로드/결과 조회 로직만 재사용
- 새로운 학습 내비게이션 카드 UI 구현

## Phase C (3~5주): 추론 품질 고도화
- 학기 누적 신호 가중치
- 다중 에이전트 합의 점수 계산
- 신뢰도 설명(왜 high인지) 생성

## Phase D (지속): 운영 최적화
- 모델 비용/토큰 예산 튜닝
- 성능 모니터링 및 실패 재시도 정책
- B2B/API 연동 대비 스키마 안정화

---

## 5) 실행 우선순위 백로그
1. 서비스 용어집(금지어/권장어) 확정
2. 출력 정책 테스트케이스 작성(원문 재현 방지)
3. 대시보드 와이어프레임 확정
4. 리포트 JSON v2 스키마 정의
5. 프론트 재작성 스프린트 시작
6. 다중 에이전트 합의 로직 실험

---

## 6) 성공 지표(KPI)
- 법적/정책 지표
  - 원문 재현 필터 트리거율, 차단 성공률
  - 세션 만료 삭제 준수율
- 제품 지표
  - 분석 완료율, 결과 열람률, 우선순위 카드 저장률
  - 학습 계획 전환율(카드 → 체크리스트)
- 품질 지표
  - 근거 매핑 성공률(오디오+교재)
  - 사용자 신뢰도 평가(결과 수용도)

---

## 7) 의사결정 원칙 (팀 합의용)
- 기술은 유지하되, **출력/UX/정책**을 먼저 바꾼다.
- "될 수 있으면 제공"보다 "법적/제품 정체성에 맞으면 제공"을 우선한다.
- 프론트는 부분 수정이 아니라 정보 구조 기준으로 재작성한다.
- 모든 신규 기능은 "교재 대체 방지" 체크리스트를 통과해야 배포한다.
